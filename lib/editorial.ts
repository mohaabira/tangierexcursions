import {cleanRich,richPlain} from './rich-content';
import content from '@/data/editorial-content.json';
export function editorialContent(page:any){const base=(content as any)[page.path]||{html:page.content||'',toc:[],words:0,minutes:1,published:'',author:'',category:'Travel planning',relatedTours:[]};return {...base,...page.editorial,category:page.category||base.category,relatedTours:page.editorial?.relatedTours??base.relatedTours,quickAnswer:page.editorial?.quickAnswer||''}}
export function editorialSummary(page:any){const e=editorialContent(page);return {relatedTours:e.relatedTours,category:e.category,published:e.published,author:e.author,minutes:e.minutes,featured:!!e.featured,order:e.order||0,excerpt:page.editorialIntro||page.meta?.description||''}}
export function validateEditorial(v:any,knownTours:string[]){if(!v)return undefined;const text=(x:any,max:number)=>String(x||'').trim().slice(0,max);if(v.relatedTours!==undefined&&(!Array.isArray(v.relatedTours)||v.relatedTours.length>12||v.relatedTours.some((p:any)=>!knownTours.includes(p))))throw Error('Choose related experiences from the tour catalogue');if(v.published&&!/^\d{4}-\d{2}-\d{2}(?:T.*)?$/.test(v.published))throw Error('Choose a valid publication date');let html=v.html===undefined?undefined:cleanRich(v.html);const toc:any[]=[];if(html!==undefined)html=html.replace(/<h([234])([^>]*)>([\s\S]*?)<\/h\1>/g,(_all,level,attrs,body)=>{const existing=attrs.match(/id="([^"]+)"/);const id=existing?.[1]||'section-'+(toc.length+1);toc.push({id,level:Number(level),title:richPlain(body)});return '<h'+level+attrs+(existing?'':' id="'+id+'"')+'>'+body+'</h'+level+'>'});const words=html===undefined?undefined:richPlain(html).split(/\s+/).filter(Boolean).length;return {...(html===undefined?{}:{html,toc,words,minutes:Math.max(1,Math.ceil(words!/200))}),featured:!!v.featured,order:Math.max(0,Math.min(999,Number(v.order)||0)),quickAnswer:text(v.quickAnswer,1800),...(v.relatedTours===undefined?{}:{relatedTours:v.relatedTours}),...(v.author===undefined?{}:{author:text(v.author,200)}),...(v.published?{published:v.published}:{}),showPlanning:v.showPlanning!==false}}
