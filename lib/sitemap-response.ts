import {sitemapPages} from './seo-platform';
const esc=(s:string)=>s.replace(/[<>&'\"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]!));
export async function sitemapResponse(kind:string){const pages=await sitemapPages(kind);const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">${pages.map((p:any)=>`<url><loc>${esc(p.url)}</loc>${p.lastModified?`<lastmod>${esc(p.lastModified)}</lastmod>`:''}${p.images.map((i:string)=>`<image:image><image:loc>${esc(i.startsWith('http')?i:'https://www.tangierexcursions.com'+i)}</image:loc></image:image>`).join('')}</url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml; charset=utf-8','Cache-Control':'public, max-age=300','X-Robots-Tag':'noindex'}})}
