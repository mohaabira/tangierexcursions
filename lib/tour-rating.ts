import {preservedSchema} from './schema-preservation';
export function auditedTourRating(blocks:string[]=[]){let result:any=null;function visit(value:any){if(!value||typeof value!=='object')return;if(['Product','TouristTrip','Service'].includes(value['@type'])&&value.aggregateRating){const r=value.aggregateRating,rating=Number(r.ratingValue),count=Number(r.reviewCount||r.ratingCount);if(rating>0&&rating<=5&&Number(r.bestRating||5)===5&&Number.isInteger(count)&&count>0)result={rating,reviewCount:count,reviewLabel:r.reviewCount?'reviews':'ratings',reviewSource:'Published tour rating'}}for(const item of Object.values(value))if(typeof item==='object')visit(item)}for(const block of blocks)try{visit(JSON.parse(preservedSchema(block)))}catch{}return result}
