export function OrganicLinks({links=[]}:any){const groups=Object.groupBy(links,(r:any)=>r.module||'Useful Travel Information');return <aside className="section" aria-label="Related travel information">{Object.entries(groups).map(([name,items]:any)=><section key={name}><h2>{name}</h2><ul>{items.map((r:any)=><li key={r.to}><a href={r.to}>{r.label}</a></li>)}</ul></section>)}</aside>}
