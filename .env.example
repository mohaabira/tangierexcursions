# Server-only 32-byte AES key encoded as 64 hexadecimal characters.
# Required for passenger information. Never expose through public environment variables.
PASSPORT_ENCRYPTION_KEY=

# Enable only after configuring customer auth and testing on the final HTTPS host.
CUSTOMER_AUTH_ENABLED=false
